# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0
python-docx>=1.1.2

# LLM Clients
openai>=1.50.0
google-generativeai>=0.8.0
anthropic>=0.40.0

# Database
supabase>=2.5.0
qdrant-client>=1.11.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.9

# Security
cryptography>=42.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Monitoring
prometheus-client>=0.21.0

# Cloud Marketplace SDKs
boto3>=1.35.0
google-cloud-aiplatform>=1.68.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# Payment
stripe>=10.0.0

# Utils
pyyaml>=6.0.2
httpx>=0.27.0
redis>=5.1.0
python-multipart>=0.0.12
jinja2>=3.1.4
aiofiles>=24.1.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0

# Global Copilots — document parsing/generation (SOC2, ISO27001, Contract Risk, Vendor Risk, EU AI Act)
pypdf>=5.1.0
reportlab>=4.2.5

# AgentVerse (Fetch.ai uAgents) -- agent identity/messaging for src/agents/agentverse_handler.py
uagents-core>=0.1.0
Pillow>=11.0.0

# LangGraph -- real execution engine for src/agents/voice_call_intelligence.py
# (post-call intent classification). Was only in requirements.txt (wrong file,
# ignored by the Dockerfile) -- same bug class as uagents-core before it.
langgraph>=0.2.0

# A2A Protocol -- src/a2a_bridge/setup.py. Was only pinned in pyproject.toml
# (~=0.3.22, also unused by the Dockerfile, and too old anyway --
# add_a2a_routes_to_fastapi doesn't exist before 1.x) -- same bug class as
# uagents-core/langgraph above: never actually installed in production, so
# A2A routes silently never mounted (caught by app/main.py's try/except).
a2a-sdk[http-server]>=1.1.0,<1.2.0